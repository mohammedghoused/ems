# Database Credentials
# Do NOT place your real passwords in this file.
# Configure these in your IDE environment variables (e.g. IntelliJ Run Configuration).
# Alternatively, you can pass them via terminal: 
# Windows: set DB_PASSWORD=your_password && mvnw spring-boot:run
# Mac/Linux: DB_PASSWORD=your_password ./mvnw spring-boot:run

DB_URL=jdbc:mysql://localhost:3306/ems
DB_USERNAME=root
DB_PASSWORD=your_secure_password_here
